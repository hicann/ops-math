<!-- Generated By CANNBot -->

# StridedSliceAssign

## 产品支持情况

| 产品                                                         | 是否支持 |
| :----------------------------------------------------------- | :------: |
| <term>Ascend 950PR/Ascend 950DT</term>                     |     √    |
| <term>Atlas A3 训练系列产品/Atlas A3 推理系列产品</term>    |    √     |
| <term>Atlas A2 训练系列产品/Atlas A2 推理系列产品</term>    |    √     |
| <term>Atlas 200I/500 A2 推理产品</term>                      |    ×     |
| <term>Atlas 推理系列产品</term>                               |    ×     |
| <term>Atlas 训练系列产品</term>                               |    ×     |

## 功能说明

- 算子功能：StridedSliceAssign是一种张量切片赋值操作，将输入张量inputValue的内容赋值给目标张量var中由begin、end、strides指定的切片位置，切片以外的区域保持var原值不变。输出var与输入var同名，为in-place语义，输出shape与输入var的shape相同。

- 计算公式：

$$
out[b_0:e_0:s_0, b_1:e_1:s_1, \dots] = inputValue
$$

切片第i维的长度为：$sliceLen[i] = \max(0, \lceil\frac{end[i] - begin[i]}{strides[i]}\rceil)$，其中$\lceil x \rceil$表示对$x$向上取整。$begin$和$end$为经过负索引归一化与边界截断后的取值：当$x < 0$时$x = x + dim$，之后截断到$[0, dim]$区间。

## 参数说明

<table style="undefined;table-layout: fixed; width: 1162px"><colgroup>
<col style="width: 139px">
<col style="width: 148px">
<col style="width: 532px">
<col style="width: 267px">
<col style="width: 76px">
</colgroup>
<thead>
  <tr>
    <th>参数名</th>
    <th>输入/输出/属性</th>
    <th>描述</th>
    <th>数据类型</th>
    <th>数据格式</th>
  </tr></thead>
<tbody>
  <tr>
    <td>var</td>
    <td>输入</td>
    <td>待切片赋值的目标tensor。</td>
    <td>FLOAT16、FLOAT、BFLOAT16、INT32、INT16</td>
    <td>ND</td>
  </tr>
  <tr>
    <td>begin</td>
    <td>输入</td>
    <td>切片位置的起始索引，1维常量tensor，长度为var的维数。</td>
    <td>INT64</td>
    <td>ND</td>
  </tr>
  <tr>
    <td>end</td>
    <td>输入</td>
    <td>切片位置的终止索引，1维常量tensor，长度为var的维数。</td>
    <td>INT64</td>
    <td>ND</td>
  </tr>
  <tr>
    <td>strides</td>
    <td>输入</td>
    <td>切片的步长，1维常量tensor，长度为var的维数。</td>
    <td>INT64</td>
    <td>ND</td>
  </tr>
  <tr>
    <td>input_value</td>
    <td>输入</td>
    <td>替换切片内容的tensor，dtype需与var保持一致，shape需严格等于计算得出的切片shape。</td>
    <td>FLOAT16、FLOAT、BFLOAT16、INT32、INT16</td>
    <td>ND</td>
  </tr>
  <tr>
    <td>var</td>
    <td>输出</td>
    <td>切片赋值后的结果tensor，与输入var同名，shape与dtype和输入var一致。</td>
    <td>FLOAT16、FLOAT、BFLOAT16、INT32、INT16</td>
    <td>ND</td>
  </tr>
  <tr>
    <td>begin_mask</td>
    <td>属性</td>
    <td>int类型，第i位为1时忽略begin[i]，起始索引取该区间的起点0。默认值为0。</td>
    <td>-</td>
    <td>-</td>
  </tr>
  <tr>
    <td>end_mask</td>
    <td>属性</td>
    <td>int类型，第i位为1时忽略end[i]，终止索引取该区间的终点dim。默认值为0。</td>
    <td>-</td>
    <td>-</td>
  </tr>
  <tr>
    <td>ellipsis_mask</td>
    <td>属性</td>
    <td>int类型，至多一位为1，表示该位展开为完整维度的切片。默认值为0。</td>
    <td>-</td>
    <td>-</td>
  </tr>
  <tr>
    <td>new_axis_mask</td>
    <td>属性</td>
    <td>int类型，第i位为1时在该位置插入长度为1的新维。默认值为0。</td>
    <td>-</td>
    <td>-</td>
  </tr>
  <tr>
    <td>shrink_axis_mask</td>
    <td>属性</td>
    <td>int类型，第i位为1时强制end[i]=begin[i]+1且stride为1，收缩该维。默认值为0。</td>
    <td>-</td>
    <td>-</td>
  </tr>
</tbody></table>

## 约束说明

- begin、end、strides为值依赖输入，必须能在host侧获取具体数值。
- 输入var支持1D~8D，begin、end、strides的长度必须等于var的维数。
- strides[i]必须大于0，且最内维的stride必须为1。
- input_value的dtype必须与var一致，shape必须严格等于切片计算后的final shape，不支持广播。
- 输出var与输入var为in-place语义，输出shape等于输入var的shape。
- 本算子为纯数据搬运算子，结果位级精确，无精度损失。

## 调用说明

<table><thead>
  <tr>
    <th>调用方式</th>
    <th>调用样例</th>
    <th>说明</th>
  </tr></thead>
<tbody>
  <tr>
    <td>图模式调用</td>
    <td><a href="./examples/test_geir_strided_slice_assign.cpp">test_geir_strided_slice_assign</a></td>
    <td>参见<a href="../../docs/zh/invocation/quick_op_invocation.md">算子调用</a>完成算子编译和验证。</td>
  </tr>
</tbody>
</table>
