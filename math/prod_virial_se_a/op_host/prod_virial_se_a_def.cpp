/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file prod_virial_se_a_def.cpp
 * \brief Operator definition for prod_virial_se_a operator
 *
 * 5输入: net_deriv, in_deriv, rij (float32/float16), nlist, natoms (int32)
 * 2输出: virial, atom_virial (float32/float16)
 * 2属性: n_a_sel, n_r_sel (REQUIRED Int)
 * dtype组合: float32全浮点 + int32索引 / float16全浮点 + int32索引
 */

#include "register/op_def_registry.h"

namespace ops {
class ProdVirialSeA : public OpDef {
public:
    explicit ProdVirialSeA(const char* name) : OpDef(name)
    {
        // 输入：net_deriv（float32/float16）
        this->Input("net_deriv")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT, ge::DT_FLOAT16})
            .Format({ge::FORMAT_ND, ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND, ge::FORMAT_ND})
            .AutoContiguous();

        // 输入：in_deriv（dtype 必须与 net_deriv 一致）
        this->Input("in_deriv")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT, ge::DT_FLOAT16})
            .Format({ge::FORMAT_ND, ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND, ge::FORMAT_ND})
            .AutoContiguous();

        // 输入：rij（dtype 必须与 net_deriv 一致）
        this->Input("rij")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT, ge::DT_FLOAT16})
            .Format({ge::FORMAT_ND, ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND, ge::FORMAT_ND})
            .AutoContiguous();

        // 输入：nlist（固定 int32）
        this->Input("nlist")
            .ParamType(REQUIRED)
            .DataType({ge::DT_INT32, ge::DT_INT32})
            .Format({ge::FORMAT_ND, ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND, ge::FORMAT_ND})
            .AutoContiguous();

        // 输入：natoms（固定 int32，值依赖 OPTIONAL）
        this->Input("natoms")
            .ParamType(REQUIRED)
            .DataType({ge::DT_INT32, ge::DT_INT32})
            .Format({ge::FORMAT_ND, ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND, ge::FORMAT_ND})
            .ValueDepend(OPTIONAL)
            .AutoContiguous();

        // 输出：virial
        this->Output("virial")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT, ge::DT_FLOAT16})
            .Format({ge::FORMAT_ND, ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND, ge::FORMAT_ND})
            .AutoContiguous();

        // 输出：atom_virial
        this->Output("atom_virial")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT, ge::DT_FLOAT16})
            .Format({ge::FORMAT_ND, ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND, ge::FORMAT_ND})
            .AutoContiguous();

        // 属性：n_a_sel（必选）
        this->Attr("n_a_sel").AttrType(REQUIRED).Int();

        // 属性：n_r_sel（必选）
        this->Attr("n_r_sel").AttrType(REQUIRED).Int();

        // AICore 配置
        OpAICoreConfig aicoreConfig;
        aicoreConfig.DynamicCompileStaticFlag(true)
            .DynamicFormatFlag(false)
            .DynamicRankSupportFlag(true)
            .DynamicShapeSupportFlag(true)
            .NeedCheckSupportFlag(false)
            .PrecisionReduceFlag(true)
            .ExtendCfgInfo("opFile.value", "prod_virial_se_a_apt");
        this->AICore().AddConfig("ascend950", aicoreConfig);
    }
};
OP_ADD(ProdVirialSeA);
} // namespace ops
