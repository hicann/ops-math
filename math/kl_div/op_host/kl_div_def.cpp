/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file kl_div_def.cpp
 * \brief KLDiv 路由算子定义：reduction 为 REQUIRED（对齐 v1 原型）、无 log_target、
 *        dtype 纸面声明含 DT_DOUBLE（对齐 v1，tiling 白名单拒绝 double）、opFile 指向自有 apt
 */

#include <cstdint>
#include "register/op_def_registry.h"

namespace ops {
// dtype 纸面声明对齐 v1 原型（DT_FLOAT16/DT_FLOAT/DT_DOUBLE/DT_BF16）；
// DT_DOUBLE 为纸面声明，实际支持以 binary config 12 组合为准（bf16/fp16/fp32），
// double 图在 tiling 阶段被 dtype 白名单显式拒绝
static const std::vector<ge::DataType> dataType = {ge::DT_FLOAT16, ge::DT_FLOAT, ge::DT_DOUBLE, ge::DT_BF16};

static const std::vector<ge::Format> dataFormat = {ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND};

static const std::vector<ge::Format> unknownShapeFormat = {ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND};
class KLDiv : public OpDef {
public:
    explicit KLDiv(const char* name) : OpDef(name)
    {
        this->Input("x")
            .ParamType(REQUIRED)
            .DataType(dataType)
            .Format(dataFormat)
            .UnknownShapeFormat(unknownShapeFormat);
        this->Input("target")
            .ParamType(REQUIRED)
            .DataType(dataType)
            .Format(dataFormat)
            .UnknownShapeFormat(unknownShapeFormat);
        this->Output("y")
            .ParamType(REQUIRED)
            .DataType(dataType)
            .Format(dataFormat)
            .UnknownShapeFormat(unknownShapeFormat);
        // v1 原型为 REQUIRED_ATTR(reduction, String)（必选无默认值），不声明 log_target（幽灵属性）
        this->Attr("reduction").AttrType(REQUIRED).String();

        OpAICoreConfig aicoreConfig;
        aicoreConfig.DynamicCompileStaticFlag(true)
            .DynamicRankSupportFlag(true)
            .DynamicShapeSupportFlag(true)
            .ExtendCfgInfo("opFile.value", "kl_div_apt");
        this->AICore().AddConfig("ascend950", aicoreConfig);
    }
};

OP_ADD(KLDiv);
} // namespace ops
