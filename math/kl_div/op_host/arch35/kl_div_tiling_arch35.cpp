/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file kl_div_tiling_arch35.cpp
 * \brief KLDiv tiling
 */

#include <map>
#include <cmath>
#include "kl_div_tiling_arch35.h"
#include "log/log.h"
#include "util/platform_util.h"
#include "register/op_impl_registry.h"
#include "atvoss/broadcast/broadcast_tiling.h"
#include "../../../kl_div_v2/op_kernel/kl_div_v2_tiling_data.h"
#include "../../../kl_div_v2/op_kernel/kl_div_v2_dag.h"
#include "../../op_kernel/kl_div_tiling_key.h"

namespace optiling {
using namespace Ops::Base;

static const int32_t ATTR_INDEX_REDUCTION = 0;
static const std::map<std::string, uint32_t> STR_2_INT = {{"none", 0}, {"mean", 1}, {"sum", 2}, {"batchmean", 3}};
static const int32_t SIZE2 = 2;
static const int32_t SIZE4 = 4;
static const float INVALID_VAL = -0.1f;

static ge::graphStatus CheckDtype(gert::TilingContext* context)
{
    OP_LOGD(context->GetNodeName(), "KLDiv CheckDtype enter.");
    auto inputXDesc = context->GetInputDesc(0);
    OP_CHECK_NULL_WITH_CONTEXT(context, inputXDesc);
    auto inputTargetDesc = context->GetInputDesc(1);
    OP_CHECK_NULL_WITH_CONTEXT(context, inputTargetDesc);
    OP_CHECK_IF(inputXDesc->GetDataType() != inputTargetDesc->GetDataType(),
                OP_LOGE_FOR_INVALID_DTYPES_WITH_REASON(context->GetNodeName(), "input x and target",
                                                       (Ops::Base::ToString(inputXDesc->GetDataType()) + " and " +
                                                        Ops::Base::ToString(inputTargetDesc->GetDataType()))
                                                           .c_str(),
                                                       "The dtypes of input x and target must be the same");
                , return ge::GRAPH_FAILED);
    return ge::GRAPH_SUCCESS;
}

// x/target 同 shape 校验（对齐 v1 verifier 的 CheckInputsShapeDtypeSame shape 维度）：
// "none" 路径按元素计算仅参数化 x 的 shape，target shape 不同会越界读，必须显式拦截
static ge::graphStatus CheckShape(gert::TilingContext* context)
{
    OP_LOGD(context->GetNodeName(), "KLDiv CheckShape enter.");
    auto xShape = context->GetInputShape(0);
    OP_CHECK_NULL_WITH_CONTEXT(context, xShape);
    auto targetShape = context->GetInputShape(1);
    OP_CHECK_NULL_WITH_CONTEXT(context, targetShape);
    const gert::Shape& xStorageShape = xShape->GetStorageShape();
    const gert::Shape& targetStorageShape = targetShape->GetStorageShape();
    OP_CHECK_IF(xStorageShape != targetStorageShape,
                OP_LOGE_FOR_INVALID_SHAPES_WITH_REASON(
                    context->GetNodeName(), "x and target",
                    (Ops::Base::ToString(xStorageShape) + " and " + Ops::Base::ToString(targetStorageShape)).c_str(),
                    "The shapes of input x and target must be the same"),
                return ge::GRAPH_FAILED);
    return ge::GRAPH_SUCCESS;
}

static ge::graphStatus SetEmptyValue(gert::TilingContext* context, KLDivV2TilingData* tilingData,
                                     const std::string reduction)
{
    auto x_shape = context->GetInputShape(0);
    OP_CHECK_NULL_WITH_CONTEXT(context, x_shape);
    const gert::Shape& input_shape = EnsureNotScalar(x_shape->GetStorageShape());

    float emptyValue = INVALID_VAL;
    for (size_t i = 0; i < input_shape.GetDimNum(); i++) {
        if (input_shape.GetDim(i) == 0) {
            if (reduction == "sum") {
                emptyValue = 0.0f;
            } else if (reduction == "mean") {
                emptyValue = std::nan("");
            } else if (reduction == "batchmean") {
                if (i == 0) {
                    emptyValue = std::nan("");
                } else {
                    emptyValue = 0.0f;
                }
            }
            break;
        }
    }
    tilingData->emptyValue = emptyValue;

    if (reduction == "batchmean" && input_shape.GetDim(0) != 0) {
        // 模板计算的均值是针对所有轴的，batchMean语义不一样
        tilingData->reduceTiling.meanVar = 1.0 / input_shape.GetDim(0);
    }
    return ge::GRAPH_SUCCESS;
}

// dtype 白名单 SIZE2/SIZE4 分派逐行保留
static ge::graphStatus DoTilingAscendC(gert::TilingContext* context, const KLDivCompileInfo* compileInfo,
                                       ReduceOpInputParam& opInput, ReduceTilingKey& key,
                                       ReduceOpTilingData* tilingData, const std::string reduction)
{
    ge::graphStatus status = ge::GRAPH_FAILED;

    if (reduction == "sum" || reduction == "none") {
        if (ge::GetSizeByDataType(opInput.inputDtype) == SIZE4) {
            status = Tiling4ReduceOp<KLDivV2::KLDivDagSumLogFalse<float, float>::OpDag>(
                context, opInput, key, &compileInfo->opInfo, tilingData);
        } else if (ge::GetSizeByDataType(opInput.inputDtype) == SIZE2) {
            status = Tiling4ReduceOp<KLDivV2::KLDivDagSumLogFalse<half, float>::OpDag>(
                context, opInput, key, &compileInfo->opInfo, tilingData);
        }
    } else { // mean / batchmean
        if (ge::GetSizeByDataType(opInput.inputDtype) == SIZE4) {
            status = Tiling4ReduceOp<KLDivV2::KLDivDagMeanLogFalse<float, float>::OpDag>(
                context, opInput, key, &compileInfo->opInfo, tilingData);
        } else if (ge::GetSizeByDataType(opInput.inputDtype) == SIZE2) {
            status = Tiling4ReduceOp<KLDivV2::KLDivDagMeanLogFalse<half, float>::OpDag>(
                context, opInput, key, &compileInfo->opInfo, tilingData);
        }
    }

    OP_CHECK_IF((status == ge::GRAPH_FAILED),
                OP_LOGE_FOR_INVALID_DTYPE(context->GetNodeName(), "x", Ops::Base::ToString(opInput.inputDtype).c_str(),
                                          "bfloat16, float16 or float"),
                return ge::GRAPH_FAILED);
    return status;
}

static ge::graphStatus Tiling4KLDivAscendC(gert::TilingContext* context, const KLDivCompileInfo* compileInfo)
{
    OP_LOGD(context->GetNodeName(), "Enter Tiling4KLDivAscendC.");
    const gert::RuntimeAttrs* attrs = context->GetAttrs();
    OP_CHECK_NULL_WITH_CONTEXT(context, attrs);
    // reduction 为唯一属性（index 0，REQUIRED）；GetStr 返回指针判空为防御式兜底
    // （REQUIRED 缺失时 GE 图校验已拦截）
    const char* reductionStr = attrs->GetStr(ATTR_INDEX_REDUCTION);
    OP_CHECK_NULL_WITH_CONTEXT(context, reductionStr);
    const std::string reduction = std::string(reductionStr);

    // logTarget 不读取：v1 原型未声明 log_target、老 kernel 从不消费（幽灵属性），
    ReduceOpInputParam opInput;
    if (reduction == "none") {
        OP_CHECK_IF((ReduceOpTmpl::GetInputParam(context, opInput, 0) == ge::GRAPH_FAILED),
                    OP_LOGE(context->GetNodeName(), "[None] ReduceOp get x input param failed"),
                    return ge::GRAPH_FAILED);
        opInput.axes.clear();
    } else {
        OP_CHECK_IF((ReduceOpTmpl::GetInputParam(context, opInput, 0, 1, 0) == ge::GRAPH_FAILED),
                    OP_LOGE(context->GetNodeName(), "ReduceOp get x input param failed"), return ge::GRAPH_FAILED);
    }
    OP_CHECK_IF(CheckDtype(context) == ge::GRAPH_FAILED, OP_LOGE(context, "check dtype failed"),
                return ge::GRAPH_FAILED);
    OP_CHECK_IF(CheckShape(context) == ge::GRAPH_FAILED, OP_LOGE(context, "check shape failed"),
                return ge::GRAPH_FAILED);

    ReduceTilingKey key;
    auto tilingData = context->GetTilingData<KLDivV2TilingData>();
    OP_CHECK_IF((DoTilingAscendC(context, compileInfo, opInput, key, &(tilingData->reduceTiling), reduction) ==
                 ge::GRAPH_FAILED),
                OP_LOGE(context->GetNodeName(), "DoTiling Failed for KLDiv"), return ge::GRAPH_FAILED);

    OP_CHECK_IF(SetEmptyValue(context, tilingData, reduction) == ge::GRAPH_FAILED,
                OP_LOGE(context, "SetEmptyValue failed"), return ge::GRAPH_FAILED);

    auto it = STR_2_INT.find(reduction);
    OP_CHECK_IF(it == STR_2_INT.end(),
                OP_LOGE_FOR_INVALID_VALUE_WITH_REASON(context->GetNodeName(), "reduction", reduction.c_str(),
                                                      "The value of reduction must be in (none, mean, sum, batchmean)"),
                return ge::GRAPH_FAILED);
    uint64_t tilingKey;
    // 壳 TilingKey 无 logTarget 维：GEN_REDUCE_TILING_KEY 四参数（V2 为五参数）
    GEN_REDUCE_TILING_KEY(tilingKey, key, it->second);
    OP_LOGI(context->GetNodeName(),
            "patternID:%u, loopARCount:%u, loopInnerARCount:%u, reduction is:%u Tiling Key is:%lu", key.patternID,
            key.loopARCount, key.loopInnerARCount, it->second, tilingKey);
    context->SetTilingKey(tilingKey);
    return ge::GRAPH_SUCCESS;
}

// 注册名换 KLDiv（V2 为 KLDivV2Tiling）；加 static
static ge::graphStatus KLDivTiling(gert::TilingContext* context)
{
    OP_LOGD(context->GetNodeName(), "Enter KLDivTiling");
    auto compile_info = reinterpret_cast<const KLDivCompileInfo*>(context->GetCompileInfo());
    OP_CHECK_NULL_WITH_CONTEXT(context, compile_info);

    return Tiling4KLDivAscendC(context, compile_info);
}

// 加 static；函数体逐行复制 V2（平台信息预取与 0 值校验零改动）
template <typename ContextT>
static ge::graphStatus TilingPrepare4ReduceOp(ContextT* context, ReduceOpCompileInfo* compileInfo)
{
    OP_CHECK_NULL_WITH_CONTEXT(context, compileInfo);
    auto platformInfo = context->GetPlatformInfo();
    OP_CHECK_NULL_WITH_CONTEXT(context, platformInfo);
    auto ascendcPlatform = platform_ascendc::PlatformAscendC(platformInfo);
    compileInfo->vectorCoreNum = ascendcPlatform.GetCoreNumAiv();
    OP_CHECK_IF((compileInfo->vectorCoreNum == 0UL),
                OP_LOGE(context->GetNodeName(), "ReduceOp GetHardwareInfo Failed, vectorCoreNum:%lu",
                        compileInfo->vectorCoreNum),
                return ge::GRAPH_FAILED);

    uint64_t ubSize = 0;
    ascendcPlatform.GetCoreMemSize(platform_ascendc::CoreMemType::UB, ubSize);
    OP_CHECK_IF((ubSize <= CACHE_BUF_SIZE),
                OP_LOGE(context->GetNodeName(), "ReduceOp GetHardwareInfo Failed, ubSize:%lu, at least:%lu.", ubSize,
                        CACHE_BUF_SIZE),
                return ge::GRAPH_FAILED);
    compileInfo->ubSize = ubSize;

    compileInfo->cacheLineSize = GetCacheLineSize(context);
    OP_CHECK_IF((compileInfo->cacheLineSize == 0UL),
                OP_LOGE(context->GetNodeName(), "ReduceOp GetHardwareInfo Failed, cacheLineSize:%lu.",
                        compileInfo->cacheLineSize),
                return ge::GRAPH_FAILED);

    compileInfo->ubBlockSize = GetUbBlockSize(context);
    OP_CHECK_IF(
        (compileInfo->ubBlockSize == 0UL),
        OP_LOGE(context->GetNodeName(), "ReduceOp GetHardwareInfo Failed, ubBlockSize:%lu.", compileInfo->ubBlockSize),
        return ge::GRAPH_FAILED);

    compileInfo->vRegSize = GetVRegSize(context);
    OP_CHECK_IF(
        (compileInfo->vRegSize == 0UL),
        OP_LOGE(context->GetNodeName(), "ReduceOp GetHardwareInfo Failed, vRegSize:%lu.", compileInfo->vRegSize),
        return ge::GRAPH_FAILED);

    OP_LOGD(context->GetNodeName(), "GetCoreNum:%lu, ubSize:%lu, cacheLineSize:%lu, ubBlockSize:%lu, vRegSize:%lu",
            compileInfo->vectorCoreNum, compileInfo->ubSize, compileInfo->cacheLineSize, compileInfo->ubBlockSize,
            compileInfo->vRegSize);

    return ge::GRAPH_SUCCESS;
}

template <typename T>
inline static T* GetCompileInfoPtr(gert::TilingParseContext* context)
{
    return context->GetCompiledInfo<T>();
}

// 类型名换 KLDivCompileInfo（V2 为 KLDivV2Parse/KLDivV2CompileInfo）
static ge::graphStatus KLDivParse(gert::TilingParseContext* context)
{
    auto compile_info = GetCompileInfoPtr<KLDivCompileInfo>(context);
    OP_CHECK_NULL_WITH_CONTEXT(context, compile_info);
    return TilingPrepare4ReduceOp(context, &compile_info->opInfo);
}

// 无值依赖
IMPL_OP_OPTILING(KLDiv).Tiling(KLDivTiling).TilingParse<KLDivCompileInfo>(KLDivParse);
} // namespace optiling
