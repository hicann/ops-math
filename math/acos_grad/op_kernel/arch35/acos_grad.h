/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

/*!
 * \file acos_grad.h
 * \brief KernelAcosGrad class implementation
 *
 * Inputs (aligned with proto.h / def.cpp):
 *   y  : forward input of Acos (canndev INPUT(y))
 *   dy : upstream gradient   (canndev INPUT(dy))
 *   z  : output gradient     (canndev OUTPUT(z))
 *
 * Formula:
 *   z = -dy / sqrt(1 - y^2)
 *
 * Out-of-domain (|y| > 1): y^2 > 1 -> 1 - y^2 < 0 -> sqrt(negative) = NaN
 *
 * FP16/BF16 path: Cast -> float32 compute -> Cast back to original dtype
 * FP32 path: direct compute
 */

#ifndef ACOS_GRAD_H
#define ACOS_GRAD_H

#include "kernel_operator.h"
#include "kernel_tiling/kernel_tiling.h"
#include "acos_grad_tiling_data.h"
#include "acos_grad_tiling_key.h"

namespace NsAcosGrad {

using namespace AscendC;

template <typename T>
class KernelAcosGrad {
    static constexpr int32_t BUFFER_NUM = 2;
    static constexpr int32_t TMP_BUFFER_NUM = 1;

public:
    __aicore__ inline KernelAcosGrad() {}

    __aicore__ inline void Init(GM_ADDR y, GM_ADDR dy, GM_ADDR z,
                                 const AcosGradTilingData* tilingData);
    __aicore__ inline void Process();

private:
    __aicore__ inline void ProcessBlock(uint32_t ubLoop, uint32_t ubTail);
    __aicore__ inline void CopyIn(int64_t progress, uint32_t currentNum);
    __aicore__ inline void Compute(uint32_t currentNum);
    __aicore__ inline void CopyOut(int64_t progress, uint32_t currentNum);

private:
    TPipe pipe_;

    TQue<TPosition::VECIN, BUFFER_NUM>  inQueueY_;
    TQue<TPosition::VECIN, BUFFER_NUM>  inQueueDy_;
    TQue<TPosition::VECOUT, BUFFER_NUM> outQueueZ_;

    TBuf<TPosition::VECCALC> yF32Buf_;
    TBuf<TPosition::VECCALC> dyF32Buf_;
    TBuf<TPosition::VECCALC> tmpF32ABuf_;
    TBuf<TPosition::VECCALC> tmpF32BBuf_;

    GlobalTensor<T>     yGm_;
    GlobalTensor<T>     dyGm_;
    GlobalTensor<T>     zGm_;

    uint32_t blockIdx_    = 0;
    uint32_t blockFormer_ = 0;
    uint32_t ubFormer_    = 0;
    int64_t  blockOffset_ = 0;
    uint32_t blockLength_ = 0;
};

template <typename T>
__aicore__ inline void KernelAcosGrad<T>::Init(
    GM_ADDR y, GM_ADDR dy, GM_ADDR z,
    const AcosGradTilingData* tilingData)
{
    blockIdx_    = GetBlockIdx();
    blockFormer_ = tilingData->blockFormer;
    ubFormer_    = tilingData->ubFormer;

    uint64_t totalLength = tilingData->totalLength;
    uint64_t start       = static_cast<uint64_t>(blockIdx_) * blockFormer_;

    if (start >= totalLength) {
        blockLength_ = 0;
        return;
    }

    uint64_t remaining = totalLength - start;
    blockLength_       = static_cast<uint32_t>((remaining < blockFormer_) ? remaining : blockFormer_);
    blockOffset_       = static_cast<int64_t>(start);

    yGm_.SetGlobalBuffer((__gm__ T*)y + blockOffset_, blockLength_);
    dyGm_.SetGlobalBuffer((__gm__ T*)dy + blockOffset_, blockLength_);
    zGm_.SetGlobalBuffer((__gm__ T*)z + blockOffset_, blockLength_);

    pipe_.InitBuffer(inQueueY_,   BUFFER_NUM, ubFormer_ * sizeof(T));
    pipe_.InitBuffer(inQueueDy_,  BUFFER_NUM, ubFormer_ * sizeof(T));
    pipe_.InitBuffer(outQueueZ_,  BUFFER_NUM, ubFormer_ * sizeof(T));

    pipe_.InitBuffer(yF32Buf_,    ubFormer_ * sizeof(float));
    pipe_.InitBuffer(dyF32Buf_,   ubFormer_ * sizeof(float));
    pipe_.InitBuffer(tmpF32ABuf_, ubFormer_ * sizeof(float));
    pipe_.InitBuffer(tmpF32BBuf_, ubFormer_ * sizeof(float));
}

template <typename T>
__aicore__ inline void KernelAcosGrad<T>::CopyIn(int64_t progress, uint32_t currentNum)
{
    LocalTensor<T> yLocal  = inQueueY_.template AllocTensor<T>();
    LocalTensor<T> dyLocal = inQueueDy_.template AllocTensor<T>();

    AscendC::DataCopyExtParams copyParams{1, static_cast<uint32_t>(currentNum * sizeof(T)), 0, 0, 0};
    AscendC::DataCopyPadExtParams<T> padParams{false, 0, 0, T(0)};

    DataCopyPad(yLocal, yGm_[progress * static_cast<int64_t>(ubFormer_)],
                copyParams, padParams);
    DataCopyPad(dyLocal, dyGm_[progress * static_cast<int64_t>(ubFormer_)],
                copyParams, padParams);

    inQueueY_.EnQue(yLocal);
    inQueueDy_.EnQue(dyLocal);
}

template <typename T>
__aicore__ inline void KernelAcosGrad<T>::Compute(uint32_t currentNum)
{
    LocalTensor<T> yLocal  = inQueueY_.template DeQue<T>();
    LocalTensor<T> dyLocal = inQueueDy_.template DeQue<T>();
    LocalTensor<T> zLocal  = outQueueZ_.template AllocTensor<T>();

    LocalTensor<float> yF32    = yF32Buf_.Get<float>();
    LocalTensor<float> dyF32   = dyF32Buf_.Get<float>();
    LocalTensor<float> tmpF32A = tmpF32ABuf_.Get<float>();
    LocalTensor<float> tmpF32B = tmpF32BBuf_.Get<float>();

    Cast(yF32, yLocal, RoundMode::CAST_NONE, currentNum);
    PipeBarrier<PIPE_V>();
    Cast(dyF32, dyLocal, RoundMode::CAST_NONE, currentNum);
    PipeBarrier<PIPE_V>();

    Mul(tmpF32A, yF32, yF32, currentNum);
    PipeBarrier<PIPE_V>();

    Muls(tmpF32A, tmpF32A, static_cast<float>(-1.0f), currentNum);
    PipeBarrier<PIPE_V>();

    Adds(tmpF32B, tmpF32A, static_cast<float>(1.0f), currentNum);
    PipeBarrier<PIPE_V>();

    Sqrt(tmpF32B, tmpF32B, currentNum);
    PipeBarrier<PIPE_V>();

    Muls(dyF32, dyF32, static_cast<float>(-1.0f), currentNum);
    PipeBarrier<PIPE_V>();

    Div(dyF32, dyF32, tmpF32B, currentNum);
    PipeBarrier<PIPE_V>();

    Cast(zLocal, dyF32, RoundMode::CAST_RINT, currentNum);
    PipeBarrier<PIPE_V>();

    outQueueZ_.template EnQue<T>(zLocal);
    inQueueY_.FreeTensor(yLocal);
    inQueueDy_.FreeTensor(dyLocal);
}

template <typename T>
__aicore__ inline void KernelAcosGrad<T>::CopyOut(int64_t progress, uint32_t currentNum)
{
    LocalTensor<T> zLocal = outQueueZ_.template DeQue<T>();

    int64_t gmOffset = progress * static_cast<int64_t>(ubFormer_);
    AscendC::DataCopyExtParams copyParams{1, static_cast<uint32_t>(currentNum * sizeof(T)), 0, 0, 0};
    DataCopyPad(zGm_[gmOffset], zLocal, copyParams);

    outQueueZ_.FreeTensor(zLocal);
}

template <typename T>
__aicore__ inline void KernelAcosGrad<T>::ProcessBlock(uint32_t ubLoop, uint32_t ubTail)
{
    for (uint32_t i = 0; i < ubLoop; i++) {
        CopyIn(static_cast<int64_t>(i), ubFormer_);
        Compute(ubFormer_);
        CopyOut(static_cast<int64_t>(i), ubFormer_);
    }
    if (ubTail > 0) {
        CopyIn(static_cast<int64_t>(ubLoop), ubTail);
        Compute(ubTail);
        CopyOut(static_cast<int64_t>(ubLoop), ubTail);
    }
}

template <typename T>
__aicore__ inline void KernelAcosGrad<T>::Process()
{
    if (blockLength_ == 0) {
        return;
    }

    uint32_t ubLoop = blockLength_ / ubFormer_;
    uint32_t ubTail = blockLength_ % ubFormer_;

    ProcessBlock(ubLoop, ubTail);
}

template <>
class KernelAcosGrad<float> {
    static constexpr int32_t BUFFER_NUM = 2;
    static constexpr int32_t TMP_BUFFER_NUM = 1;

public:
    __aicore__ inline KernelAcosGrad() {}

    __aicore__ inline void Init(GM_ADDR y, GM_ADDR dy, GM_ADDR z,
                                 const AcosGradTilingData* tilingData);
    __aicore__ inline void Process();

private:
    __aicore__ inline void ProcessBlock(uint32_t ubLoop, uint32_t ubTail);
    __aicore__ inline void CopyIn(int64_t progress, uint32_t currentNum);
    __aicore__ inline void Compute(uint32_t currentNum);
    __aicore__ inline void CopyOut(int64_t progress, uint32_t currentNum);

private:
    TPipe pipe_;

    TQue<TPosition::VECIN, BUFFER_NUM>  inQueueY_;
    TQue<TPosition::VECIN, BUFFER_NUM>  inQueueDy_;
    TQue<TPosition::VECOUT, BUFFER_NUM> outQueueZ_;

    TQue<TPosition::VECCALC, TMP_BUFFER_NUM> tmpQueue1_;
    TQue<TPosition::VECCALC, TMP_BUFFER_NUM> tmpQueue2_;

    GlobalTensor<float> yGm_;
    GlobalTensor<float> dyGm_;
    GlobalTensor<float> zGm_;

    uint32_t blockIdx_    = 0;
    uint32_t blockFormer_ = 0;
    uint32_t ubFormer_    = 0;
    int64_t  blockOffset_ = 0;
    uint32_t blockLength_ = 0;
};

__aicore__ inline void KernelAcosGrad<float>::Init(
    GM_ADDR y, GM_ADDR dy, GM_ADDR z,
    const AcosGradTilingData* tilingData)
{
    blockIdx_    = GetBlockIdx();
    blockFormer_ = tilingData->blockFormer;
    ubFormer_    = tilingData->ubFormer;

    uint64_t totalLength = tilingData->totalLength;
    uint64_t start       = static_cast<uint64_t>(blockIdx_) * blockFormer_;

    if (start >= totalLength) {
        blockLength_ = 0;
        return;
    }

    uint64_t remaining = totalLength - start;
    blockLength_       = static_cast<uint32_t>((remaining < blockFormer_) ? remaining : blockFormer_);
    blockOffset_       = static_cast<int64_t>(start);

    yGm_.SetGlobalBuffer((__gm__ float*)y + blockOffset_, blockLength_);
    dyGm_.SetGlobalBuffer((__gm__ float*)dy + blockOffset_, blockLength_);
    zGm_.SetGlobalBuffer((__gm__ float*)z + blockOffset_, blockLength_);

    pipe_.InitBuffer(inQueueY_,   BUFFER_NUM, ubFormer_ * sizeof(float));
    pipe_.InitBuffer(inQueueDy_,  BUFFER_NUM, ubFormer_ * sizeof(float));
    pipe_.InitBuffer(outQueueZ_,  BUFFER_NUM, ubFormer_ * sizeof(float));
    pipe_.InitBuffer(tmpQueue1_,  TMP_BUFFER_NUM, ubFormer_ * sizeof(float));
    pipe_.InitBuffer(tmpQueue2_,  TMP_BUFFER_NUM, ubFormer_ * sizeof(float));
}

__aicore__ inline void KernelAcosGrad<float>::CopyIn(int64_t progress, uint32_t currentNum)
{
    LocalTensor<float> yLocal  = inQueueY_.template AllocTensor<float>();
    LocalTensor<float> dyLocal = inQueueDy_.template AllocTensor<float>();

    AscendC::DataCopyExtParams copyParams{1, static_cast<uint32_t>(currentNum * sizeof(float)), 0, 0, 0};
    AscendC::DataCopyPadExtParams<float> padParams{false, 0, 0, 0.0f};

    DataCopyPad(yLocal, yGm_[progress * static_cast<int64_t>(ubFormer_)],
                copyParams, padParams);
    DataCopyPad(dyLocal, dyGm_[progress * static_cast<int64_t>(ubFormer_)],
                copyParams, padParams);

    inQueueY_.EnQue(yLocal);
    inQueueDy_.EnQue(dyLocal);
}

__aicore__ inline void KernelAcosGrad<float>::Compute(uint32_t currentNum)
{
    LocalTensor<float> yLocal  = inQueueY_.template DeQue<float>();
    LocalTensor<float> dyLocal = inQueueDy_.template DeQue<float>();
    LocalTensor<float> zLocal  = outQueueZ_.template AllocTensor<float>();

    LocalTensor<float> tmpA = tmpQueue1_.template AllocTensor<float>();
    LocalTensor<float> tmpB = tmpQueue2_.template AllocTensor<float>();

    Mul(tmpA, yLocal, yLocal, currentNum);
    PipeBarrier<PIPE_V>();

    Muls(tmpA, tmpA, static_cast<float>(-1.0f), currentNum);
    PipeBarrier<PIPE_V>();

    Adds(tmpB, tmpA, static_cast<float>(1.0f), currentNum);
    PipeBarrier<PIPE_V>();

    Sqrt(tmpB, tmpB, currentNum);
    PipeBarrier<PIPE_V>();

    Muls(tmpA, dyLocal, static_cast<float>(-1.0f), currentNum);
    PipeBarrier<PIPE_V>();

    Div(zLocal, tmpA, tmpB, currentNum);

    outQueueZ_.template EnQue<float>(zLocal);
    inQueueY_.FreeTensor(yLocal);
    inQueueDy_.FreeTensor(dyLocal);
    tmpQueue1_.FreeTensor(tmpA);
    tmpQueue2_.FreeTensor(tmpB);
}

__aicore__ inline void KernelAcosGrad<float>::CopyOut(int64_t progress, uint32_t currentNum)
{
    LocalTensor<float> zLocal = outQueueZ_.template DeQue<float>();

    int64_t gmOffset = progress * static_cast<int64_t>(ubFormer_);
    AscendC::DataCopyExtParams copyParams{1, static_cast<uint32_t>(currentNum * sizeof(float)), 0, 0, 0};
    DataCopyPad(zGm_[gmOffset], zLocal, copyParams);

    outQueueZ_.FreeTensor(zLocal);
}

__aicore__ inline void KernelAcosGrad<float>::ProcessBlock(uint32_t ubLoop, uint32_t ubTail)
{
    for (uint32_t i = 0; i < ubLoop; i++) {
        CopyIn(static_cast<int64_t>(i), ubFormer_);
        Compute(ubFormer_);
        CopyOut(static_cast<int64_t>(i), ubFormer_);
    }
    if (ubTail > 0) {
        CopyIn(static_cast<int64_t>(ubLoop), ubTail);
        Compute(ubTail);
        CopyOut(static_cast<int64_t>(ubLoop), ubTail);
    }
}

__aicore__ inline void KernelAcosGrad<float>::Process()
{
    if (blockLength_ == 0) {
        return;
    }

    uint32_t ubLoop = blockLength_ / ubFormer_;
    uint32_t ubTail = blockLength_ % ubFormer_;

    ProcessBlock(ubLoop, ubTail);
}

} // namespace NsAcosGrad

#endif // ACOS_GRAD_H
