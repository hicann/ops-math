/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file sparse_bincount_apt.cpp
 * \brief Kernel entry for sparse_bincount operator.
 *        Scene mode dispatched via TilingKey (is1D x binaryOutput x hasWeights = 8 variants).
 *        dtype dispatched via DTYPE_VALUES / DTYPE_SIZE macros.
 *
 * TilingKey encoding: schMode = is1D * 4 + binaryOutput * 2 + hasWeights
 *   0: ND + count + no_weight
 *   1: ND + count + weight
 *   2: ND + binary + no_weight
 *   3: ND + binary + weight
 *   4: 1D + count + no_weight
 *   5: 1D + count + weight
 *   6: 1D + binary + no_weight
 *   7: 1D + binary + weight
 */

#include "arch35/sparse_bincount_simt.h"

template <uint32_t schMode>
__global__ __aicore__ void sparse_bincount(
    GM_ADDR indices, GM_ADDR values, GM_ADDR dense_shape,
    GM_ADDR size, GM_ADDR weights, GM_ADDR output,
    GM_ADDR workspace, GM_ADDR tiling)
{
    REGISTER_TILING_DEFAULT(SparseBincountTilingData);
    GET_TILING_DATA_WITH_STRUCT(SparseBincountTilingData, tilingData, tiling);

    // Decode scene mode from tiling key
    constexpr bool IS_1D = (schMode >= 4);
    constexpr bool BINARY_OUTPUT = ((schMode & 0x2) != 0);
    constexpr bool HAS_WEIGHTS = ((schMode & 0x1) != 0);

    NsSparseBincount::Process<DTYPE_VALUES, DTYPE_SIZE, IS_1D, BINARY_OUTPUT, HAS_WEIGHTS>(
        indices, values, dense_shape, size, weights, output, &tilingData);
}
