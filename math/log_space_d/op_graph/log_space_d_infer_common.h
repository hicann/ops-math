/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

#ifndef LOG_SPACE_D_INFER_COMMON_H
#define LOG_SPACE_D_INFER_COMMON_H

#include "op_common/log/log.h"
#include "register/op_impl_registry.h"

#include <cmath>
#include <cstddef>

namespace ge {

namespace {
constexpr size_t kInputIndex = 0;
constexpr size_t kOutputIndex = 0;
constexpr size_t kStartAttrIndex = 0;
constexpr size_t kEndAttrIndex = 1;
constexpr size_t kBaseAttrIndex = 3;
constexpr size_t kDtypeAttrIndex = 4;
} // namespace

inline graphStatus InferLogSpaceDDataType(gert::InferDataTypeContext* context)
{
    if (context == nullptr) {
        OP_LOGE("LogSpaceD", "InferDataType context is null");
        return GRAPH_FAILED;
    }
    const ge::DataType inputType = context->GetInputDataType(kInputIndex);
    if (inputType != ge::DT_FLOAT16 && inputType != ge::DT_FLOAT && inputType != ge::DT_BF16) {
        OP_LOGE(context->GetNodeName(), "LogSpaceD InferDataType rejects unsupported input dtype=%d",
                static_cast<int>(inputType));
        return GRAPH_FAILED;
    }
    const gert::RuntimeAttrs* attrs = context->GetAttrs();
    if (attrs == nullptr) {
        OP_LOGE(context->GetNodeName(), "LogSpaceD InferDataType runtime attributes are null");
        return GRAPH_FAILED;
    }
    const float* start = attrs->GetFloat(kStartAttrIndex);
    const float* end = attrs->GetFloat(kEndAttrIndex);
    const float* base = attrs->GetFloat(kBaseAttrIndex);
    if (start == nullptr || end == nullptr || base == nullptr) {
        OP_LOGE(context->GetNodeName(), "LogSpaceD InferDataType required start/end/base attribute is missing");
        return GRAPH_FAILED;
    }
    if (!std::isfinite(*start) || !std::isfinite(*end) || !std::isfinite(*base)) {
        OP_LOGE(context->GetNodeName(), "LogSpaceD InferDataType requires finite start=%f end=%f base=%f", *start, *end,
                *base);
        return GRAPH_FAILED;
    }
    const int64_t* dtypeAttr = attrs->GetInt(kDtypeAttrIndex);
    if (dtypeAttr == nullptr) {
        OP_LOGE(context->GetNodeName(), "LogSpaceD InferDataType dtype attribute is missing");
        return GRAPH_FAILED;
    }
    if (*dtypeAttr != 0 && *dtypeAttr != 1) {
        OP_LOGE(context->GetNodeName(), "LogSpaceD InferDataType dtype attribute must be 0 or 1, got=%ld",
                static_cast<long>(*dtypeAttr));
        return GRAPH_FAILED;
    }
    const ge::DataType expectedOutputType = inputType;
    const ge::DataType declaredOutputType = context->GetOutputDataType(kOutputIndex);
    if (declaredOutputType != ge::DT_UNDEFINED && declaredOutputType != ge::DT_MAX &&
        declaredOutputType != expectedOutputType) {
        OP_LOGE(context->GetNodeName(), "LogSpaceD InferDataType output dtype conflict: declared=%d expected=%d",
                static_cast<int>(declaredOutputType), static_cast<int>(expectedOutputType));
        return GRAPH_FAILED;
    }
    const graphStatus status = context->SetOutputDataType(kOutputIndex, expectedOutputType);
    if (status != GRAPH_SUCCESS) {
        OP_LOGE(context->GetNodeName(), "LogSpaceD InferDataType failed to set output dtype, status=%u",
                static_cast<unsigned int>(status));
    }
    return status;
}

} // namespace ge

#endif // LOG_SPACE_D_INFER_COMMON_H
