/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

#pragma once

#include <cstdint>

// Shared Host/Kernel ABI for S0=FP16, S1=FP32, S2=BF16 and the unified empty
// template. All lengths are element counts unless stated otherwise.
struct LogSpaceDTilingData {
    std::int64_t dim0; // Valid element count; equals steps.
    // Signed base-mode carrier: >0 positive, <0 negative, 0 zero base;
    // the absolute value is the candidate core count.
    std::int64_t coreNum;
    std::int64_t blockFormer; // Elements in each non-tail block.
    std::int64_t blockNum;    // Number of launched blocks.
    std::int64_t blockTail;   // Valid elements in the tail block.

    std::int64_t ubFormer;            // Elements in a full UB tile.
    std::int64_t ubLoopOfFormerBlock; // UB loops for a non-tail block.
    std::int64_t ubTailOfFormerBlock; // Valid elements in its final tile.
    std::int64_t ubLoopOfTailBlock;   // UB loops for the tail block.
    std::int64_t ubTailOfTailBlock;   // Valid elements in its final tile.

    std::int64_t storageElemBytes;   // GM bytes per element.
    std::int64_t bufferBytesPerElem; // Total UB bytes per element.
    std::int64_t halfway;            // First index of the end-anchored exponent segment.

    float start;      // FP32 exponent start.
    float end;        // FP32 exponent end.
    float step;       // FP32 exponent step.
    float logBase;    // FP32 natural logarithm of abs(base); 0 for base==0.
    float stepLow;    // Compatibility ABI field; fixed to 0 for all paths.
    float logBaseLow; // FP32 residual of ln(abs(base)).
    float base;       // Original FP32 base used by the FP32 Power path.
};
