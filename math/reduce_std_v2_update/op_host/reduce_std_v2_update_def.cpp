/**
 * Copyright (c) 2025-2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file reduce_std_v2_update_def.cpp
 * \brief ReduceStdV2Update 算子定义（输入输出、dtype、format 配置）
 *
 * 输入：x, mean（二者 shape/dtype 一致，mean 已 Expand 广播到 x shape）
 * 输出：output_var（方差或标准差，dtype 与 x 一致）
 * 属性：dim(list[int64]), if_std(bool), unbiased(bool), keepdim(bool), correction(int64)
 *
 * 注：REG_OP(ReduceStdV2Update) 已在 canndev 原型注册（输出名 output_var），
 *   本 OpDef 沿用相同输入输出名以保持与 GEIR 一致。
 */

#include "register/op_def_registry.h"

namespace ops {
class ReduceStdV2Update : public OpDef {
public:
    explicit ReduceStdV2Update(const char* name) : OpDef(name)
    {
        this->Input("x")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT, ge::DT_FLOAT16, ge::DT_BF16})
            .Format({ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND})
            .AutoContiguous();

        this->Input("mean")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT, ge::DT_FLOAT16, ge::DT_BF16})
            .Format({ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND})
            .AutoContiguous();

        this->Output("output_var")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT, ge::DT_FLOAT16, ge::DT_BF16})
            .Format({ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND})
            .AutoContiguous();

        // dim 为 REQUIRED_ATTR（值依赖，tiling 阶段读取做合轴决策）
        // 无参数 ListInt() 表示 REQUIRED_ATTR（与 REG_OP REQUIRED_ATTR(dim, ListInt) 等价）
        this->Attr("dim").ListInt();
        this->Attr("if_std").Bool(false);
        this->Attr("unbiased").Bool(true);
        this->Attr("keepdim").Bool(false);
        this->Attr("correction").Int(1);

        OpAICoreConfig aiCoreConfig;
        aiCoreConfig.DynamicCompileStaticFlag(true)
            .DynamicFormatFlag(false)
            .DynamicRankSupportFlag(true)
            .DynamicShapeSupportFlag(true)
            .NeedCheckSupportFlag(false)
            .PrecisionReduceFlag(true)
            .ExtendCfgInfo("opFile.value", "reduce_std_v2_update_apt");
        this->AICore().AddConfig("ascend950", aiCoreConfig);
    }
};
OP_ADD(ReduceStdV2Update);
} // namespace ops
